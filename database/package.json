{
  "name": "@aicamera/database",
  "version": "1.0.0",
  "description": "Shared database configuration for AI Camera microservices",
  "private": true,
  "scripts": {
    "migrate": "prisma migrate dev",
    "migrate:prod": "prisma migrate deploy",
    "generate": "prisma generate",
    "studio": "prisma studio",
    "seed": "tsx seed.ts",
    "reset": "prisma migrate reset",
    "format": "prisma format"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1"
  },
  "devDependencies": {
    "prisma": "^5.3.1",
    "tsx": "^3.14.0",
    "typescript": "^5.3.3"
  }
}